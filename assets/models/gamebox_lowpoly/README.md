# GameBox Low-Poly Starter Assets

Small bundled GLB models generated for the GameBox 3D runner prototype. They avoid the broken OBJ/MTL import path and are scanned automatically by `scripts/Main.gd`.

Categories are detected from file names: character, crate/box, barrier, cone, building, lamp, road.
